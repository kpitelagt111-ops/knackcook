# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# prisma generated client (if using custom output)
/lib/generated

# env files (NEVER commit secrets)
.env
.env*.local
.env.production

# media (own media is served from VPS filesystem, not committed)
/media

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# testing
/coverage
/playwright-report
/test-results

# IDE
.vscode/*
!.vscode/extensions.json
.idea

# TypeScript incremental build cache
tsconfig.tsbuildinfo



# === Added 2026-05-31 — opencode/sisyphus internal state (NEVER commit) ===
.agents/
.omo/
.playwright-mcp/

# Session artifacts (screenshots, verify outputs)
verify-*.png
audit-*.png
audit2-*.png
audit3-*.png
audit-final-*.png
final-*.png
reaudit-*.png

# Local server logs (from running .next/standalone/server.js locally)
server.log
server.pid
.next/server.*.log
.next/server.pid
